# Hero Video Regression — Restore Previous Behavior

The latest iOS hero video fix changed the intended experience too much.

The hero video is a core brand element and must remain the primary hero background.

## Required behavior

Restore the previous hero video behavior.

Do NOT:

* disable the YouTube iframe on iOS by default
* replace the video permanently with a static image
* add any “Play video” button
* add any overlay CTA for video playback
* redesign the hero

## What should happen

1. The hero YouTube video should be attempted on all devices, including iPhone.
2. The fallback image should exist only as a loading/background safety layer.
3. If the iframe takes time to load, fallback can show underneath.
4. But the video must remain the intended primary visual.
5. No visible Play button should be added.

## Important

This used to work before the recent change, so please identify exactly what changed and revert only the part that disabled the video on iOS.

Keep:

* improved z-index fix if it does not block the video
* fallback image as a loading layer only
* desktop and Android behavior unchanged
* audio system unchanged

## Deliverable

Send:

1. exact root cause of the regression
2. files changed
3. confirmation that iOS no longer automatically skips the iframe
4. confirmation no Play button was added
5. confirmation fallback is only a background/loading layer
6. confirmation desktop/Android still work

# Critical Cross-Device Production QA — iOS Audio Instability + Cyprus YouTube Hero Issue

We still have two production issues that must be fixed properly and finally.

Do NOT make random changes.
Do NOT redesign anything.
Do NOT break Android/Windows behavior, which currently works.

---

## Issue 1 — iPhone Israel: Audio is unreliable

Current behavior:

* Android Israel: audio works
* Windows Israel: audio works
* iPhone Israel: audio sometimes works, but most of the time it does not
* Refreshing does not consistently fix it

This means the iOS audio implementation is still timing-sensitive or state-dependent.

### Required investigation

Debug specifically on iPhone/Safari:

1. Is the YouTube audio player fully ready before the modal button is clicked?
2. Is `playVideo()` always called synchronously inside the actual tap handler?
3. Is there any async delay between the tap and playback?
4. Is the preloaded player sometimes not ready, causing fallback behavior?
5. Are there two possible player paths competing?
6. Is `activePlayerType` always correct?
7. Are mute/unmute states correct before `playVideo()`?
8. Are there Safari console errors or rejected play attempts?
9. Does refresh create stale state in module-level variables?
10. Is the audio prompt/modal firing before the YouTube IFrame API is actually ready?

### Required fix

Make iPhone/Safari playback deterministic:

* either delay the “Enable Sound” button until the player is ready
* or show a “Preparing audio…” state until ready
* or ensure fallback cannot race/fail on iOS

The user must not be able to tap Enable Sound before the iOS-safe player path is ready.

---

## Issue 2 — Cyprus iPhone/iPad: Hero YouTube video stuck on YouTube logo

Current behavior:

* Israel Android: hero video works
* Israel Windows: hero video works
* Israel iPhone: hero video works
* Cyprus iPhone: hero video stuck on YouTube logo
* Cyprus iPad: hero video stuck on YouTube logo
* Audio works in Cyprus

This looks like a region/device/player restriction or CDN/player-load issue with YouTube iframe background video.

### Required investigation

Check:

1. Is this video restricted by region, age, embed permissions, or playback policy in Cyprus?
2. Does the YouTube video allow embedding in all regions?
3. Does `youtube-nocookie.com` behave differently from `youtube.com`?
4. Are googlevideo media requests blocked/failing in Cyprus iOS?
5. Does the iframe load but fail to start playback?
6. Does iOS in Cyprus require a different fallback strategy?
7. Is the current YouTube video URL/video ID region-restricted?

---

## Required solution for hero video

The hero video must remain the primary experience.

However, if YouTube refuses to play on a specific iOS/region combination, we need a professional fallback that does not show a stuck YouTube logo.

Required behavior:

* Always attempt the YouTube iframe first.
* Keep fallback image behind it.
* If iframe does not successfully start/render after a timeout on iOS, hide the iframe and show the premium fallback image.
* Do NOT show a YouTube logo stuck state.
* Do NOT add a visible Play button.
* Do NOT redesign the hero.

This preserves the video-first approach while preventing broken visual states.

---

## Important constraints

Do NOT:

* remove YouTube hero video globally
* disable video by default on iOS
* add a play button overlay
* change the hero design
* break audio
* break Android/Windows behavior
* change SEO pages

---

## Deliverable

Send a precise report with:

1. root cause of iPhone audio instability
2. root cause of Cyprus iPhone/iPad hero video issue, or best-confirmed diagnosis
3. files changed
4. exact fix applied for audio
5. exact fix applied for hero video stuck-logo state
6. confirmation:

   * Android Israel still works
   * Windows Israel still works
   * iPhone Israel audio works consistently
   * iPhone Israel hero video still works
   * iPhone/iPad Cyprus no longer shows stuck YouTube logo
   * audio still works in Cyprus

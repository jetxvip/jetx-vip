# JetX.vip — Final SEO QA and Validation

The SEO page system is now built.

Your task in this prompt is NOT to add content and NOT to refactor.

Your only task is to perform a full final validation of the multilingual SEO rollout and fix only real issues found during QA.

---

## Scope

Validate the complete SEO page system across:

* routing
* rendering
* metadata
* hreflang
* sitemap
* language handling
* visual integrity
* navigation isolation

Do NOT redesign anything.
Do NOT add new features.
Do NOT change public styling.

---

## QA Checklist

### 1. Slug audit

Verify the exact final slug count.

You reported **41 slugs** total.
Confirm whether that is intentional.

Check for:

* accidental duplicates
* logical duplicates
* duplicate route intent
* duplicate SEO purpose

If 41 is correct, explain why.
If one slug should be removed, identify it clearly.

---

### 2. Route validation

Verify that every SEO slug resolves correctly in all 4 languages:

* en
* he
* ar
* ru

That means all expected routes exist:

`/{lang}/{slug}`

Check for:

* 404s
* missing pages
* missing language variants
* broken related links

---

### 3. Metadata validation

For every SEO page version, verify:

* title exists
* meta description exists
* canonical exists
* Open Graph title/description exists
* correct page-specific metadata is used

Check for:

* duplicated titles
* duplicated descriptions
* missing metadata
* metadata using wrong language

---

### 4. hreflang validation

Verify every SEO page outputs correct hreflang alternates for:

* en
* he
* ar
* ru
* x-default

Check for:

* broken alternate URLs
* missing alternates
* wrong canonical/hreflang combinations

---

### 5. Sitemap validation

Verify `sitemap.xml` includes:

* core pages
* all SEO pages
* all 4 language versions of each SEO slug

Check the final expected count and report it.

---

### 6. Visual integrity

Confirm that the SEO pages use the current JetX.vip premium design system and did not alter the existing public site design.

Check for:

* layout breaks
* wrong spacing
* broken cards
* poor RTL rendering
* incorrect typography
* visual inconsistency between languages

---

### 7. Navigation isolation

Confirm that these SEO pages:

* do NOT appear in the main navigation
* do NOT clutter the visible UI
* remain secondary landing pages only

---

### 8. Internal linking

Verify:

* relatedSlugs links work
* quick links work
* links point to correct localized routes
* no broken SEO-page links exist

---

### 9. Language / direction audit

Confirm:

* Hebrew = RTL
* Arabic = RTL
* English = LTR
* Russian = LTR

Check that each SEO page renders correctly in its proper language direction.

---

### 10. Design safety

Confirm no design changes were introduced to:

* homepage
* about
* services
* fleet
* contact
* quote
* empty legs
* ambulance
* admin/public layout

---

## Fix policy

If you find a real issue:

* fix only that issue
* do not refactor unrelated code
* do not redesign anything

---

## Final deliverable

When done, send me:

1. exact total slug count
2. exact total route count across all 4 languages
3. exact sitemap count
4. whether 41 slugs is correct or needs cleanup
5. list of any issues found
6. exact files changed (if any)
7. confirmation that public design remains unchanged
8. confirmation that SEO pages remain outside main navigation
9. confirmation that hreflang/canonical/metadata are valid

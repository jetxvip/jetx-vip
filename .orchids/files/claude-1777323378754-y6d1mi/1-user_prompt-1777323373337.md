Great progress — this is very close to perfect.

I need one final stability fix to fully eliminate edge-case fallback triggers.

Issue:
Even with the new 10s last-resort timer, we must guarantee that the timer is cancelled as soon as ANY valid YouTube signal is received — not just iframe onLoad.

Required Fix:

1. The 10s fallback timer MUST be cleared immediately when ANY of the following occur:
   - iframe onLoad
   - YouTube player onReady
   - onStateChange = BUFFERING
   - onStateChange = PLAYING
   - ANY valid player signal (even if playback hasn’t fully started yet)

2. The timer should ONLY trigger fallback if:
   - There was absolutely ZERO signal from the iframe AND
   - ZERO signal from the YouTube player API
   - for the full 10 seconds

3. Important:
   Slow loading or delayed autoplay must NOT trigger fallback.
   As long as there is any sign of life from the player — keep the iframe.

4. Do NOT unmount or hide the iframe after it has successfully loaded or emitted any signal.

5. Fallback image behavior:
   - It should always exist behind the iframe (z-index: 0)
   - The iframe should sit above it (z-index: 1)
   - The fallback should NOT replace the iframe unless videoFailed === true

6. Only set videoFailed = true when:
   - YouTube onError fires (100 / 101 / 150)
   - OR the 10s timer completes with absolutely no signal at all

7. Do NOT introduce any retry / remount logic.
   Single stable iframe only.

Goal:
When I provide a valid YouTube URL, the hero video should ALWAYS remain mounted and visible, regardless of slow network or delayed autoplay — with zero flash and zero fallback unless there is a real failure.
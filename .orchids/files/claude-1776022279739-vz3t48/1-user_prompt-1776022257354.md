Replace the current homepage hero video with a YouTube-based background video.

You will receive the YouTube URL separately.

Goal:
Make the YouTube video behave exactly like the current background video (autoplay, muted, looping, no controls), without breaking layout or design.

CRITICAL REQUIREMENTS:

1. Extract the YouTube video ID from the provided URL.

2. Implement the video using an iframe embed with the correct parameters.

3. The video MUST:
- autoplay automatically
- be muted (no sound at all)
- loop continuously
- have no visible controls
- look like a clean background video (no YouTube UI as much as possible)

4. VERY IMPORTANT — LOOPING:
YouTube does NOT loop unless BOTH of these are set:
- loop=1
- playlist=VIDEO_ID

You MUST include:
playlist=VIDEO_ID

Example structure:
https://www.youtube.com/embed/VIDEO_ID?autoplay=1&mute=1&loop=1&playlist=VIDEO_ID&controls=0&modestbranding=1&playsinline=1&rel=0

5. Additional required parameters:
- autoplay=1
- mute=1
- controls=0
- modestbranding=1
- playsinline=1
- rel=0

6. Layout:
- The video must fully cover the hero section
- Preserve current positioning, sizing, and styling
- Maintain responsiveness (desktop + mobile)
- Do NOT change design or spacing

7. Fallback:
- Keep existing fallback image if video fails

8. Cleanup:
- Remove the old local video file from /public if no longer used

9. DO NOT:
- change any layout or styling
- change routing
- change auth or admin
- touch unrelated files

10. After implementation:
- list all files changed
- confirm autoplay works
- confirm video is muted
- confirm looping works correctly
- confirm video is no longer served from Vercel
# JetX.vip — Russian Language Implementation (Production)

We are adding a new language to an existing production React website.

## Task

Implement **Russian (`ru`)** into the existing multilingual system.

I am attaching the full Russian translation block separately.
You must integrate it exactly and safely into the current project.

---

## Critical rules

### Do not change:

* existing keys
* existing hierarchy
* existing logic
* hooks
* functions
* existing language content (`en`, `he`, `ar`)
* component structure unless required only for exposing the new language in the existing language selector

### You are allowed to:

* add `ru` to the existing translations object
* connect `ru` to the existing language switcher / language button / dropdown
* make the minimum required updates so Russian works end-to-end in the current site

---

## Implementation requirements

### 1. Add Russian to LanguageContext

* Add a new language object: `ru`
* Keep the structure **identical** to the existing translation structure
* Do not rename any keys
* Do not remove any keys
* Do not flatten or reorganize anything
* Russian must use:

```js
dir: 'ltr'
```

### 2. Use the Russian block exactly as provided

* I am pasting a ready translation block
* Use it as the source of truth
* Do not rewrite it into a weaker tone
* Do not simplify the wording
* Do not replace premium phrasing with generic wording
* Preserve terminology consistency across all sections

### 3. Tone requirement

This Russian is intentionally written for a **luxury private aviation brand**.

It must feel:

* native
* elegant
* discreet
* premium
* calm and confident

It must **not** feel:

* machine translated
* literal
* cheap
* overly aggressive
* generic marketing copy

### 4. Terminology rules

* Keep **Empty Leg** as `Empty Leg` wherever appropriate
* Preserve strong CTA wording
* Do not downgrade buttons into weak wording like:

  * `Отправить`
  * `Нажмите сюда`
* Prefer the provided premium CTA wording from the translation block

### 5. Add Russian to the existing language selector

You must also add **Russian / ru** to the existing language switcher that already exists on the site.

That includes:

* language button
* dropdown
* selector list
* any rendered language options
* any mapping/array/config used by the existing language switcher

Do this in the smallest, safest way possible.

### 6. Ensure switching works correctly

The following language flow must work:
`he → en → ar → ru → he`

No crashes.
No missing labels.
No broken navigation.
No fallback to English because of missing integration.

### 7. UI checks

After integration, verify:

* no broken layout
* no obvious text overflow
* no missing text
* no undefined keys
* no mixed English inside Russian except where intentionally preserved (for example `Empty Leg`, aircraft model names, aviation naming)

### 8. Important technical note

If the project has duplicated translation sections such as repeated keys, do not “clean up” or refactor them unless absolutely necessary for the Russian integration.
The goal is safe implementation, not refactoring.

### 9. Output expectation

Implement the Russian language so it behaves exactly like the existing languages and is available through the existing language selector.

Do not redesign anything.
Do not improve unrelated code.
Do not refactor.
Do not touch Arabic/Hebrew/English copy.
Only perform the exact integration required for Russian.

---

## Deliverable

Please:

1. integrate `ru`
2. connect it to the existing language switcher
3. make sure Russian renders correctly across the site
4. keep all current behavior intact

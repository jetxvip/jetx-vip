Audit the current project and harden the admin authentication/security without breaking anything that already works, without changing the visual design, layout, routes, branding, content, or user-facing behavior of the public site.

Important constraints:
- Do NOT break existing deployment on Vercel.
- Do NOT change the current admin path behavior except if needed to make it configurable and safer.
- Do NOT remove existing features.
- Do NOT change the look and feel of the system.
- Keep backward compatibility wherever possible.
- Make only focused, production-safe security improvements.
- Explain every change in comments and summarize all changes at the end.

Tasks:
1. Review the existing admin/auth implementation end-to-end and identify all current protections and weaknesses.
2. Keep the current secret admin route, but harden the actual security behind it.
3. Add login rate limiting / brute-force protection to the admin login endpoint:
   - limit repeated failed attempts by IP
   - temporary lockout after too many failures
   - return safe generic error messages
4. Move auth away from localStorage to secure HttpOnly cookie-based auth if possible in this architecture:
   - HttpOnly
   - Secure
   - SameSite=Strict
   - path limited appropriately
   - keep login/logout flow working
5. Reduce token/session lifetime to a safer production value and implement sliding inactivity timeout safely.
6. Add server-side logout/session invalidation behavior as much as possible for this stack.
7. Restrict CORS so only the real production domain(s) are allowed, instead of wildcard origin.
8. Add strong security headers without breaking the app:
   - Content-Security-Policy
   - X-Frame-Options / frame-ancestors
   - X-Content-Type-Options
   - Referrer-Policy
   - Permissions-Policy
   - HSTS for production HTTPS
9. Protect admin and private API routes consistently.
10. Add safe logging for admin login attempts and auth failures, without exposing secrets.
11. If feasible, add an optional second security layer for admin login such as email OTP / 2FA-ready structure, but do not break the existing login if not fully configured.
12. Ensure secrets are never exposed to the client bundle.
13. Keep all existing working public pages and admin features intact.
14. Do not visually redesign anything.
15. At the end:
   - list exactly what protections already existed
   - list what was added
   - list what could not be safely changed without a bigger refactor
   - show all modified files
   - provide copy-paste Vercel environment variables if any new ones are required
ORCHID IMPLEMENTATION PROMPT – PRODUCTION READINESS + ADMIN PANEL FULL CONNECTIVITY

Please perform a full production-readiness review and implementation pass across the entire website and admin panel.

IMPORTANT:
This is not only a visual review.
This is a full functional, technical, and deployment readiness task.

The goal is to ensure that every existing feature, every admin-controlled setting, and every dynamic website element works fully and correctly in production after deployment, not only in development or preview mode.

==================================================
MAIN GOAL
=========

Make sure the website is fully production-ready.

This includes:

* all front-end features
* all admin panel controlled content
* all legal pages
* all forms
* all language switching
* all accessibility tools
* all cookie settings
* all footer links
* all dynamic contact details
* all popups
* all submit flows
* all persistence logic
* all integrations currently built into the site

Everything that is supposed to work from the admin panel must be fully connected and fully functional in production.

Everything that appears to work locally or in preview must also work correctly after real deployment in production.

==================================================
CRITICAL REQUIREMENT
====================

Do not leave anything as “mock”, “temporary”, “placeholder logic”, “demo behavior”, “hardcoded test values”, or “UI-only without real functionality”.

If something is expected to be editable in admin:

* it must actually be editable in admin
* the live site must actually pull the value from admin
* updates from admin must actually appear correctly on the live production website

==================================================
PRODUCTION CHECKLIST – MUST VERIFY EVERYTHING
=============================================

Please verify and fix all of the following:

1. ADMIN PANEL CONNECTIVITY

* all editable texts
* all dynamic contact info
* phone numbers
* email addresses
* legal page content where applicable
* last updated dates where applicable
* content blocks
* button labels if admin-controlled
* footer data
* office locations if admin-controlled
* cookie text/settings if admin-controlled

Everything that is intended to come from admin must be correctly wired end-to-end.

2. MULTI-LANGUAGE FUNCTIONALITY
   Make sure all Hebrew / English switching works correctly in production:

* page content
* legal pages
* accessibility statement
* terms of use
* privacy policy
* footer labels
* cookie banner
* popups
* accessibility panel
* buttons
* forms
* direction (RTL / LTR)

No broken language states.
No mixed-language rendering.
No fallback bugs.

3. LEGAL COMPONENTS
   Verify production functionality of:

* Terms of Use page
* Privacy Policy page
* Accessibility Statement page
* Cookie Preferences functionality
* legal footer links
* mandatory legal checkbox popup before form submission

Make sure all legal text displays correctly in production and matches active language.

4. FORM FLOWS
   Verify all forms in production:

* contact forms
* quote forms
* booking / inquiry forms
* any lead forms
* any future shared form component

Check:

* validation works
* submit works
* legal consent modal works
* language mode works
* errors are handled properly
* success state works
* no duplicate submissions
* no broken API calls
* no missing backend connection

5. LEGAL CONSENT MODAL
   Verify the legal modal works fully in production:

* appears before every submission
* checkbox must be checked
* correct language shown
* correct legal text shown
* submission blocked until approval
* approval metadata saved if implemented
* no bypass via Enter key or alternate submission flow

6. COOKIE BANNER / CONSENT
   Verify production behavior:

* banner appears when needed
* user preferences are stored correctly
* banner does not reappear incorrectly
* settings panel works
* analytics / marketing behavior follows consent logic where implemented
* Hebrew / English content switches correctly

7. ACCESSIBILITY FEATURES
   Verify production functionality of:

* accessibility button
* accessibility panel
* all accessibility controls
* persistence of accessibility settings if intended
* accessibility statement page
* correct language switching
* proper mobile behavior

8. FOOTER
   Verify footer in production:

* all legal links work
* dynamic business info loads correctly
* legal disclaimer line appears correctly
* no broken routes
* no dead links
* no wrong language labels

9. MOBILE + DESKTOP
   Test all major flows in both:

* desktop
* mobile
* tablet if relevant

Make sure production behavior is stable across viewport sizes.

10. DEPLOYMENT STABILITY
    Before final handoff, verify:

* no production console errors
* no broken assets
* no missing environment variables
* no failing API requests
* no hydration / rendering errors
* no broken routing
* no broken images
* no admin data fetch failures
* no dependency issues affecting production build

==================================================
ADMIN PANEL REQUIREMENTS
========================

Any item that is meant to be manageable from the admin panel must be verified in the following full chain:

1. update value in admin
2. save successfully
3. value persists correctly in database / CMS / backend
4. production frontend fetches updated value
5. updated value displays correctly on live site
6. language-specific content also works correctly if applicable

Do not assume admin integration is working unless the full chain is verified.

==================================================
DO NOT LEAVE ANY OF THE FOLLOWING
=================================

* hardcoded fallback values that override admin data
* test values visible in production
* partial connections
* UI controls with no backend effect
* fields that save in admin but do not update on live site
* language values that exist in UI but do not load properly
* dates that require manual code edits unless intended
* broken production-only issues

==================================================
PRODUCTION QA REQUIREMENTS
==========================

Before deployment is considered complete, perform a full QA pass and verify:

* all major pages load correctly
* all dynamic sections render correctly
* all language switching works
* all admin updates reflect live
* all forms send correctly
* all legal flows work
* all routes are correct
* all buttons and links work
* all footer/legal elements work
* all accessibility features work
* all cookie settings work
* no visible bugs remain

==================================================
FINAL DEPLOYMENT REQUIREMENT
============================

Do not mark the task as complete until the site is genuinely ready for production use.

Before final deployment / publish:

* test the production build
* test the deployed environment
* verify all critical features again after deployment
* confirm that production works smoothly and continuously without known blocking bugs

The production site must be stable, fully connected, and operational.

==================================================
EXPECTED FINAL OUTCOME
======================

After your review and fixes:

* every feature works in production
* every admin-controlled item works end-to-end
* all dynamic content updates properly
* all multilingual behavior works correctly
* all legal and accessibility features work correctly
* deployment is clean and stable
* there are no known broken production issues

END.

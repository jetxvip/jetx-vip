JetX.vp — Final Production Readiness Audit (Critical)

We are now at the final stage before production deployment.

All features are implemented and working in development/preview.

Now I need a **full production readiness audit** to ensure that EVERYTHING works correctly in real-world production conditions.

This must be treated as a serious business deployment — not a design check.

---

## 1. Absolute requirement

Everything that works in development MUST work identically in production.

No exceptions.

This includes:

* admin panel
* authentication
* leads system
* contact system
* offers / empty legs
* dynamic content
* storage / persistence
* UI behavior

---

## 2. Authentication & Security

Verify:

* login works using Vercel environment variables ONLY
* NO fallback credentials exist in code
* NO hardcoded email/password anywhere
* JWT is properly signed using JWT_SECRET
* protected routes (/admin) are NOT accessible without valid token
* token is validated correctly on every protected request

### Session behavior:

* inactivity timeout (10 minutes) works in production
* token is cleared on expiration
* user is redirected to /login
* refresh does NOT bypass auth

---

## 3. Environment Variables (Vercel)

Verify that production depends ONLY on:

* ADMIN_EMAIL
* ADMIN_PASSWORD
* JWT_SECRET
* KV_REST_API_URL
* KV_REST_API_TOKEN

### Critical:

* system must FAIL securely if these are missing
* no silent fallback to localStorage-only without warning

---

## 4. Data Persistence & Sync (CRITICAL)

Verify:

* ALL data is saved to Vercel KV (not only localStorage)
* ALL data loads from KV on page load
* ALL changes sync across devices

### Must work for:

* leads
* contact info (phones, emails, WhatsApp)
* offers
* empty leg flights
* admin notes

### Test scenarios:

* create data on mobile → appears on desktop
* update on desktop → appears on mobile
* refresh → data remains
* open 2 tabs → data stays consistent

---

## 5. Leads System (Business critical)

Verify:

* every form submission (ALL types) creates a lead:

  * quote form
  * empty leg
  * special offer
* lead includes:

  * customer details
  * source
  * relevant context (flight / offer)

### Notifications:

* bell notification works
* sound plays
* toast appears
* works also for leads coming from OTHER devices (via KV sync)

NO lost leads. ZERO tolerance.

---

## 6. Admin Panel Functionality

Verify:

* admin loads correctly in production
* no console errors
* no broken state
* notes system works (multiple notes, timestamps)
* status updates persist
* sold-out toggle persists
* all CRUD operations work

---

## 7. Contact System (Phones & Emails)

Verify:

* multiple phones work (up to 5)
* multiple emails work (up to 5)
* labels display correctly
* only filled entries are shown
* primary number appears in key UI areas
* full list appears in contact page/footer

### Mobile:

* phones → tel: works
* emails → mailto: works

---

## 8. UI & Routing

Verify:

* all routes work in production:

  * /login
  * /admin
  * /fleet
  * /services
  * /empty-legs
  * etc.

* NO 404 on refresh (SPA routing via vercel.json works)

* RTL/LTR works correctly

* mobile / tablet / desktop all behave correctly

---

## 9. Performance & Stability

Verify:

* no unnecessary API calls
* no infinite loops
* no memory leaks
* polling (30s sync) works efficiently
* retry logic works (KV failures handled gracefully)

---

## 10. Error Handling

Verify:

* API failures do NOT break UI
* KV downtime is handled safely
* user sees clear behavior (no silent failure)
* system does not crash

---

## 11. Final Production Checklist

Before deployment:

* remove ALL dev-only code:

  * dev-api-server.mjs
  * Vite proxy
  * .env.local (not used in production)

* ensure:

  * clean build
  * no console warnings
  * no debug logs

---

## 12. Final Requirement

This is NOT a partial check.

I need confirmation that:

* the system behaves identically in production
* all features are stable
* all data is persistent and synced
* admin works securely
* no feature breaks under real usage

This must be treated as a real business-critical deployment.

Only confirm completion after verifying ALL scenarios above.

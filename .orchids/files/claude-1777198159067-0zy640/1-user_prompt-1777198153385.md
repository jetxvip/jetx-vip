# JetX.vip — Audio Experience Not Playing

The audio feature UI appears correctly:

* audio URL was added in admin
* floating speaker icon appears
* audio modal/button appears

But when clicking “Enable Sound”, no audio is heard.

Please debug and fix playback.

## Required checks

1. Verify the saved `company.audioExperienceUrl` is actually loaded on the public site.
2. Verify the URL is a direct playable audio file, ideally `.mp3`.
3. Add error handling/logging for the audio element:

   * `oncanplay`
   * `onplay`
   * `onpause`
   * `onerror`
   * rejected `audio.play()` promise
4. Check the browser console and Network tab for:

   * 404
   * CORS error
   * unsupported MIME type
   * autoplay rejection
   * mixed content HTTP/HTTPS issue
5. If the URL is not a direct audio file, show a clear admin/help message:
   “Use a direct MP3/audio file URL, not YouTube/Drive/share page links.”

## Required fix

Make the audio start reliably after the user clicks Enable Sound.

Do NOT autoplay before click.
Do NOT change design.
Do NOT refactor unrelated files.

## Deliverable

Send:

1. root cause
2. exact files changed
3. whether the tested URL was direct MP3 or not
4. confirmation that playback works after click
5. confirmation that mute/unmute works

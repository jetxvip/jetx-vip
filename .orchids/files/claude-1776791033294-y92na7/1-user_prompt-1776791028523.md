# JetX.vip — Final Next.js Cleanup (Remove Remaining Console Errors)

The public site and admin panel now load, but there are still remaining Next.js runtime / dev console errors.

I want all real migration-related errors cleaned up.

## Goal

Keep:

* the current visual design exactly as-is
* the current working admin functionality
* the SEO / SSR architecture

Fix:

* remaining Next.js console/runtime errors
* improper client/server component usage
* improper async patterns in client components
* outdated params usage
* any real hydration issues caused by app code

Do NOT redesign anything.

---

## 1) Fix async Client Component errors

There are still errors like:

* `<AdminLoginPage> is an async Client Component`
* `<AdminPanelPage> is an async Client Component`

### Required fix:

Audit the admin route modules and ensure:

* no Client Component is declared as `async`
* any async work is moved to:

  * server component layer
  * `useEffect`
  * event handlers
  * proper data-fetching boundaries
* do not create promises directly during client render

If a page must stay client-side, it must be synchronous at render time.

---

## 2) Fix “component was suspended by an uncached promise”

There is still an error saying:

* `A component was suspended by an uncached promise`

### Required fix:

Find where a promise is being created inside a Client Component or hook during render.

Move that logic out of render and into a supported pattern:

* `useEffect`
* server data loading
* cached/suspense-compatible pattern if intentionally using suspense

Important:
Do not patch around it.
Fix the actual source.

---

## 3) Fix Next.js 15 dynamic params usage

There are still admin route warnings around direct access to:

* `params.adminSlug`

### Required fix:

Update all affected admin route files to the correct Next.js 15 pattern for route params.

Audit both:

* admin login route
* admin panel route

Clean this up fully.

---

## 4) Hydration mismatch with `fdprocessedid`

There is also still a hydration warning showing an injected attribute like:

* `fdprocessedid="..."`

This looks like a browser/dev-environment injected attribute, not app-owned markup.

### Required action:

Verify whether this warning reproduces in:

* production
* incognito
* browser with extensions disabled

If it only happens in Orchids/dev or due to browser autofill/extensions, explicitly mark it as environment-only and do not over-engineer an app-side fix.

If it is app-caused, fix it properly.

---

## 5) Deliverable

When finished, send me:

1. exact list of files changed
2. exact root cause for each remaining issue
3. exact fix applied for each issue
4. confirmation that admin still works
5. confirmation that the public design is unchanged
6. which warnings/errors were true app issues vs dev-environment-only issues

---

## Final requirement

I want a clean Next.js migration with:

* working public site
* working admin
* no real migration/runtime console errors
* no design changes

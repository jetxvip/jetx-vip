You stopped again mid-task.

From now on, do NOT work in multi-file batches.

You must work in **micro-batches only**.

## New execution rule

In each run:

* complete only ONE file, or at most TWO tightly related files
* stop only after that file is fully finished
* then send me the full completed file content or a clear completion summary

Do NOT start the next file automatically.

Do NOT try to build the whole system in one pass.

---

## Right now: continue from the exact point you stopped

You already started:

* `src/data/seoPages.js`

Your only task now is:

### Task

Finish `src/data/seoPages.js` completely.

Requirements:

* keep the structure you already started
* include the data model
* include the 2 seed pages completely
* make the file valid and complete
* do not start `SeoPageTemplate.jsx`
* do not start `[lang]/[slug]/page.jsx`
* do not update sitemap yet

## Deliverable

Return only:

1. confirmation that `src/data/seoPages.js` is complete
2. the final full file content
3. nothing else

Do not continue to any next step until I explicitly tell you to continue.

# JetX.vip — Fix Hero YouTube Video in Cyprus/iOS Without Switching to MP4

The hero video must remain YouTube-based.

Do NOT switch back to local MP4 because that causes bandwidth/hosting cost issues on Vercel.

Current behavior:

* Israel Android: YouTube hero video works
* Israel Windows: works
* Israel iPhone: works
* Cyprus iPhone/iPad: stuck on YouTube logo
* Audio works

## Goal

Make the YouTube hero video work in Cyprus/iOS as well, while still loading from YouTube.

Do NOT replace the hero video with MP4.
Do NOT permanently replace it with a static image.
Do NOT add a visible play button.
Do NOT redesign the hero.

---

## Required investigation

Check whether the problem is caused by:

1. The specific YouTube video ID being region-restricted in Cyprus/iOS
2. YouTube embed policy on that video
3. iOS Safari autoplay behavior
4. Cookie/privacy restrictions
5. `youtube.com` vs `youtube-nocookie.com`
6. Missing or incorrect `origin` parameter
7. Missing `playsinline=1`
8. Missing `mute=1` / `muted=1`
9. Player API not detecting actual playback state
10. CDN/network blocking of `googlevideo.com` requests in Cyprus

---

## Required YouTube-based fixes to try

### 1. Use the most compatible embed URL

Try using:

`https://www.youtube-nocookie.com/embed/{VIDEO_ID}`

with params:

```txt
autoplay=1
mute=1
muted=1
controls=0
loop=1
playlist={VIDEO_ID}
playsinline=1
rel=0
modestbranding=1
iv_load_policy=3
enablejsapi=1
origin=https://jetx.vip
```

Also test the same with:

`https://www.youtube.com/embed/{VIDEO_ID}`

and choose whichever works more reliably on iOS/Cyprus.

---

### 2. Add a recovery mechanism

If the iframe loads but playback does not enter PLAYING or BUFFERING after 4–6 seconds:

* do NOT immediately give up
* first try to reload/recreate the iframe once
* try alternate host:

  * youtube-nocookie.com → youtube.com
  * or youtube.com → youtube-nocookie.com

Only after both attempts fail, show the fallback image.

---

### 3. Check the actual video ID

Test whether the current hero video ID has regional/embed restrictions.

If the specific video is restricted in Cyprus/iOS:

* replace it with another YouTube video ID that allows embedding globally
* keep the same visual style
* confirm it works in Cyprus/iOS

---

### 4. Do not show stuck YouTube logo

If YouTube ultimately refuses playback, the stuck logo must not remain visible.

Fallback image is allowed only after:

* YouTube iframe was attempted
* recovery attempt was tried
* alternate host was tried
* playback still failed

---

## Required behavior

Priority order:

1. Attempt YouTube iframe normally
2. If stuck, retry/recreate iframe
3. If still stuck, try alternate YouTube host
4. If still stuck, hide iframe and show premium fallback image

The video remains the primary experience.

---

## Constraints

Do NOT:

* switch to local MP4
* host video on Vercel
* add visible play button
* remove YouTube
* change hero design
* break Android/Windows/Israel behavior
* affect audio system

---

## Deliverable

Send:

1. root cause or best-confirmed diagnosis
2. whether the issue was video ID restriction, host, params, or iOS autoplay
3. files changed
4. which YouTube host works best
5. whether current video ID is safe globally
6. confirmation:

   * Israel Android works
   * Israel Windows works
   * Israel iPhone works
   * Cyprus iPhone/iPad works, or fallback appears only after all YouTube attempts fail
7. confirmation no MP4/Vercel-hosted video was introduced

Good. Continue with the next micro-batch only.

## Task

Create:

`src/app/[lang]/[slug]/page.jsx`

## Requirements

Use the existing SEO data source and template already created:

* `src/data/seoPages.js`
* `src/components/SeoPageTemplate.jsx`

This file must:

1. resolve the page by `lang` and `slug`
2. render the correct SEO page content
3. return `notFound()` when slug does not exist
4. support all 4 languages:

   * en
   * he
   * ar
   * ru
5. generate page metadata from the selected page/language
6. generate canonical URL
7. generate hreflang alternates for all 4 language versions
8. preserve the current public design
9. not touch main navigation
10. not update sitemap yet

## Important rules

* do NOT create any additional files
* do NOT refactor unrelated code
* do NOT start generating more SEO pages yet
* do NOT continue automatically to the next step

## Deliverable

Return only:

1. confirmation that `src/app/[lang]/[slug]/page.jsx` is complete
2. the final full file content
3. nothing else

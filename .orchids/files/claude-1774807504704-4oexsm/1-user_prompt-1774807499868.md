JetX.vp — Final bug fix only: public lead submissions are not reaching admin reliably

At this point, the site is working well overall and I do NOT want unnecessary changes.

What is already working correctly:

* content sync across devices
* mobile and incognito reflect admin updates correctly
* Empty Leg flights update correctly
* Special Offers update correctly
* image, price, and content updates work correctly
* local, mobile, and live site all sync correctly

So please do NOT change anything unrelated.

## The only remaining issue

When a user submits a lead from:

* mobile
* incognito
* public site as a regular visitor

the request does NOT reliably appear in the admin panel.

This includes:

* quote request
* Empty Leg inquiry
* Special Offer inquiry
* any public lead/request form

This is the final blocking issue.

---

## Required fix

### 1. Public lead submissions must be saved correctly

Any lead submitted from the public site must:

* be stored successfully
* reach persistent storage
* appear in admin

This must work from:

* normal browser
* incognito
* mobile device
* fresh session with no local state

No dependency on localStorage should be required for a lead to reach admin.

---

### 2. Admin must receive new leads without manual refresh

If the admin panel is open in the office, I should NOT need to manually refresh the page to see new incoming requests.

Required behavior:

* admin stays open
* user submits a new request from another device
* admin receives the new lead automatically

No manual refresh.

---

### 3. Notification must trigger automatically

When a new lead arrives:

* it must appear in admin automatically
* bell / unread indicator must update
* notification sound must play

This must work for leads coming from other devices, not only from the same browser session.

---

## Important implementation requirement

Please focus ONLY on the lead submission / lead delivery / live admin notification flow.

Do NOT change:

* design
* existing sync behavior that already works
* content logic
* admin features that are already stable

The goal is to fix this final issue without breaking anything else.

---

## What to verify before calling it fixed

1. Open admin panel on desktop
2. Keep admin open without refreshing
3. Submit a lead from mobile or incognito
4. Confirm:

   * lead appears in admin automatically
   * no manual refresh is needed
   * notification sound plays
   * unread/bell indicator updates

This is the final production bug to resolve.

The latest security hardening introduced two regressions in production. Please fix them WITHOUT changing the site design, layout, branding, routes, admin path, or breaking existing functionality.

Regression 1 — Protected store requests now return 401:
In production, requests such as:
- /api/store?key=jetx_clients
- /api/store?key=jetx_leads
- /api/store?key=jetx_subscribers

are returning 401 Unauthorized.

Please audit and fix the auth integration so that:
1. login successfully sets the HttpOnly cookie in production
2. all protected admin/store requests work reliably in production
3. protected requests support both:
   - HttpOnly cookie auth
   - Authorization Bearer token fallback
4. any frontend fetches that need credentials use the correct configuration
5. existing admin functionality continues to work exactly as before

Important:
- Do NOT weaken security
- Do NOT revert the hardening
- Do NOT make private store endpoints public
- Do NOT remove denylist / logout / rate limiting
- Fix the auth integration only

Regression 2 — Homepage intro/hero video no longer plays:
The homepage intro/hero video used to work before the hardening. Now only the fallback image appears.

Please audit and fix CSP safely so that:
1. the existing homepage video works again in production
2. only the minimum required media source(s) are allowed
3. all other security headers remain intact
4. do NOT broadly loosen CSP more than necessary

Also:
- ignore OTP/2FA env vars for now unless they are proven to be the root cause, because they are optional and were not intended to be required for normal login
- explain the exact root cause of the 401 issue
- explain the exact root cause of the video issue
- list all files changed
- keep visual behavior unchanged
- keep security improvements intact
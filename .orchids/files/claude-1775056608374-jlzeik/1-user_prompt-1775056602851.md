ORCHID UPDATE PROMPT – FLOATING ICON POSITION + FULLSCREEN ADMIN ANNOUNCEMENT + EMPTY LEG CARD TEXT DISPLAY

Please implement the following 3 updates on the website and admin system.

==================================================
PART 1 – MOVE THE WHATSAPP AND ACCESSIBILITY ICONS SLIGHTLY UP
==============================================================

Please raise both floating icons slightly upward on the screen:

1. WhatsApp floating button
2. Accessibility floating button

GOAL:

* Move both buttons a bit higher from the bottom edge
* Keep their current size and design
* Keep proper spacing between them
* Make sure they do not touch the footer or overlap legal links
* Make sure they do not interfere with main CTA buttons or forms

REQUIREMENTS:

* Keep the same visual style
* Keep them aligned properly
* Maintain correct behavior on desktop and mobile
* Maintain RTL / LTR positioning logic if it already exists
* Do not make them larger or smaller
* Just raise them slightly upward for better placement

==================================================
PART 2 – ADD A FULLSCREEN ADMIN-CONTROLLED SITE ANNOUNCEMENT POPUP
==================================================================

Build a new admin-controlled website announcement feature.

GOAL:
Allow me to create and publish a fullscreen popup message from the admin panel, so that when users enter the website, they see a full-screen announcement before browsing the site.

This is for messages such as:

* holiday greetings
* company announcements
* special notices
* promotions
* temporary updates
* important client-facing messages

==================================================
ADMIN FEATURE REQUIREMENTS
==========================

In the admin panel, add a new controllable feature for a site-wide popup announcement.

The admin must be able to manage:

1. ENABLE / DISABLE announcement

* toggle on/off

2. ANNOUNCEMENT TITLE

* editable text field

3. ANNOUNCEMENT BODY TEXT

* large free-text field
* allow full custom text
* allow multiple paragraphs

4. ANNOUNCEMENT IMAGE

* upload image or select image URL
* optional, not required

5. BUTTON TEXT (optional)

* optional CTA button text

6. BUTTON LINK (optional)

* optional destination if button is used

7. CLOSE BUTTON ENABLED

* always allow closing with X

8. LANGUAGE SUPPORT
   The message must support Hebrew and English separately.

Admin must be able to enter:

* Hebrew title
* Hebrew body
* Hebrew image if needed (or shared image)
* Hebrew button text

And separately:

* English title
* English body
* English button text

==================================================
FRONTEND BEHAVIOR
=================

When announcement is enabled:

* On site entry, show a fullscreen modal / overlay
* It must appear above the website and cover the full screen
* The website behind it should be blocked until the user closes the popup
* The user can close it by clicking an X button
* Once closed, the user can continue browsing the site

IMPORTANT:

* It should look premium, elegant, and branded
* It should not feel broken or spammy
* It should support both text-only and text+image mode

==================================================
ANNOUNCEMENT DISPLAY RULES
==========================

If site is in Hebrew mode:

* show Hebrew version
* RTL layout

If site is in English mode:

* show English version
* LTR layout

Do not mix languages.

==================================================
PERSISTENCE LOGIC
=================

Recommended behavior:

* show once per browser session until closed
  OR
* show once every 24 hours if enabled

But also give admin an option if possible:

* always show on every visit while enabled
  OR
* show once per session

If that option is too much, default to:

* show once per session

==================================================
DESIGN REQUIREMENTS
===================

* full-screen overlay
* elegant dark or soft branded background
* centered content card or well-designed hero-style popup
* close X clearly visible
* optional image support
* readable heading
* readable body text
* CTA button optional
* mobile responsive
* premium feel, not cheap-looking

==================================================
EXAMPLE USE CASE
================

For example, I want to be able to publish a message like:

Hebrew:
"JetX.vip מברכים אתכם לשנה החדשה..."
with any free text I want

English:
"JetX.vip wishes you a happy new year..."
with any custom text I want

This must be editable entirely from the admin panel without code changes.

==================================================
PART 3 – EMPTY LEG CARD FREE TEXT SHOULD DISPLAY ON THE FRONTEND CARD
=====================================================================

Currently in the admin panel, when adding a new Empty Leg item, there is a free-text field, but it is only internal and does not display publicly.

Please change this behavior.

GOAL:
Any free text entered for an Empty Leg item in the admin panel should appear publicly on the Empty Leg card on the website.

This should work the same way the Offers / Flash Offers card already behaves:

* when text is added in admin
* it appears visibly on the frontend card

Do the same for Empty Leg.

==================================================
EMPTY LEG TEXT DISPLAY REQUIREMENTS
===================================

* The free text entered in admin must render on the Empty Leg card
* It should appear in a clean, readable location on the card
* It should match the site design
* It should support Hebrew and English if the admin structure already supports language-specific fields
* If only one text field exists, render that text as-is
* Do not keep it internal-only anymore

==================================================
FINAL REQUIREMENTS
==================

Please implement all 3 updates:

1. Raise WhatsApp and accessibility floating buttons slightly upward
2. Add a fully admin-controlled fullscreen site announcement popup
3. Make Empty Leg admin free text visible on the frontend card, similar to Offers

Make sure all of this works in:

* Hebrew mode
* English mode
* mobile
* desktop
* production

END.

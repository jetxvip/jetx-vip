Add admin-controlled hero video URL management to the website.

Goal:
Allow me to update the homepage hero video directly from the admin panel by pasting a new YouTube URL, without changing code manually.

Current setup:
- The homepage hero video is already using a YouTube embed
- The site currently reads a hardcoded YouTube video URL / ID in the frontend
- I want this to become editable from admin

Requirements:

1. Admin Panel
- Add a dedicated field in the admin panel for:
  - Hero Video URL
- The field should accept a full YouTube URL
- Add a save/update action
- Keep the admin UI clean and simple

2. Data Storage
- Store the hero video URL in the same existing persistent store / admin data system already used by the project
- Do NOT introduce a completely separate system unless necessary
- Persist the value so it remains after redeploys

3. Frontend Behavior
- Homepage should no longer rely on a hardcoded YouTube URL
- Homepage should read the current hero video URL from the stored admin value
- If a valid new URL is saved in admin, the site should automatically use it
- If no custom URL exists, keep a safe fallback to the current default video

4. YouTube Handling
- Extract the YouTube video ID safely from the saved URL
- Build the embed URL correctly
- Preserve the current behavior:
  - autoplay=1
  - mute=1
  - controls=0
  - modestbranding=1
  - playsinline=1
  - rel=0
- If loop/playlist parameters are still needed in the current implementation, keep them working correctly with the new video ID

5. Validation
- Validate that the entered URL is a YouTube URL
- Prevent invalid/broken values from being saved if possible
- If the URL is invalid, show a clear admin error message

6. Safety
- Do NOT break existing homepage layout
- Do NOT change styling unnecessarily
- Do NOT touch authentication/security logic
- Do NOT break existing admin behavior
- Do NOT remove fallback image behavior

7. Expected files
Likely areas to update:
- admin UI component/page
- existing store/API integration
- homepage hero video logic

8. After implementation
Return:
- list of all changed files
- where the hero video URL is stored
- how fallback works
- how YouTube URL validation works
- confirmation that future video changes can be done only from admin
Before deployment, make sure the admin panel is fully included in the production app.

Requirements:

1. Create a real accessible route for the admin panel:
- /admin

2. Make sure /admin works after deployment on Vercel
- No 404 on direct access
- No 404 on refresh
- No broken route after production build

3. Configure SPA rewrites for Vercel so all frontend routes resolve correctly:
- /admin
- /fleet
- /services
- /empty-legs
- all other internal routes

4. The admin panel must be part of the production React/Vite app, not just a local Orchids preview tool.

5. Keep the existing admin functionality:
- manage flights
- manage packages
- manage WhatsApp
- manage email
- manage phone number
- manage contact/company details

6. Make sure the admin data persists after deployment and after refresh.
Do not keep admin data as temporary preview-only state.

7. Add a proper Vercel-compatible routing configuration (such as vercel.json rewrite rules) if needed.

8. Test these production cases:
- opening /admin directly
- refreshing /admin
- navigating to /admin from another page
- editing data in admin and confirming it remains saved

Goal:
After deployment, I must be able to access the admin panel reliably from:
/admin
in production, without 404 errors.
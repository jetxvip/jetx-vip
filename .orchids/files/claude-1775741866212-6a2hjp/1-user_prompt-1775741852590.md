# JetX.vip — Final Russian Premium Cleanup (Global Replace Required)

The Russian implementation exists, but the premium upgrade was applied only partially.

There are still old Russian strings remaining in `translations.ru`, while other sections already contain the upgraded premium wording. This creates inconsistent brand tone across the Russian experience.

## What must be done

Run a **global search-and-replace across the entire `ru` object only**.

Do NOT:

* change keys
* change structure
* refactor
* touch `en`, `he`, or `ar`
* touch logic

Only replace every remaining old instance with the final premium wording below.

---

## Mandatory global replacements inside `translations.ru`

Replace all remaining old instances of:

```txt id="7prg3n"
Связаться немедленно
→ Связаться с медицинским координатором
```

```txt id="528xsu"
Время реагирования
→ Время реагирования команды
```

```txt id="014q3a"
В кратчайшие сроки
→ В максимально сжатые сроки, без компромиссов
```

```txt id="858jlwm"
Частная авиация, доведённая до совершенства.
→ Частная авиация, доведённая до безупречности.
```

```txt id="6727iw"
Частная авиация, переосмысленная для тех, кто не принимает ничего меньшего.
→ Частная авиация, переосмысленная для тех, кто не признаёт компромиссов.
```

```txt id="8099y1"
Доступны 24/7 для вашего следующего путешествия
→ Доступны 24/7 для организации вашего следующего перелёта
```

```txt id="151iq7"
Услуги
→ Наши услуги
```

```txt id="703qu2"
Навигация
→ Разделы сайта
```

```txt id="222fzi"
Контакты
→ Связаться с нами
```

```txt id="477k5l"
Частный чартер
→ Частные чартерные перелёты
```

```txt id="731lm2"
VIP-услуги
→ Индивидуальные VIP-решения
```

```txt id="346yz4"
Запросить предложение
→ Запросить персональное предложение
```

```txt id="175dvh"
Оставить заявку
→ Оставить заявку на частный перелёт
```

```txt id="875a8v"
Все права защищены.
→ Все права защищены. JetX.vip
```

---

## Why this is required

The current Russian file still contains older strings like:

* `Связаться немедленно`
* `Время реагирования`
* `В кратчайшие сроки`
* `Частная авиация, доведённая до совершенства.`

while other parts of the same `ru` object already contain the upgraded premium versions.

This means the premium pass was only partially applied and must now be completed consistently across the full Russian language object.

---

## Final QA requirement

After replacement, verify that:

* no old Russian wording remains anywhere in `ru`
* no English fallback text remains in `ru` except intentionally preserved aviation/brand terms
* language cycle still works: `he → en → ar → ru → he`

The `LanguageContext` already supports `ru` in saved state and in `toggleLang`, so this task is content cleanup only.

JetX.vp — FINAL PRODUCTION GUARANTEE + KV DEPENDENCY VALIDATION

Before deployment, I need absolute confirmation that the system will behave correctly in REAL production conditions.

There are still critical risks that must be addressed.

---

## 1. KV IS NOT OPTIONAL

Current behavior:
If KV is not configured, the system falls back to localStorage.

This is NOT acceptable for production.

### Requirement:

* KV must be treated as REQUIRED in production
* If KV is missing or fails → system must NOT silently fall back
* instead:

  * show clear error
  * log failure
  * prevent false success behavior

I do NOT want a system that "looks like it works" but is actually local-only.

---

## 2. CROSS-DEVICE VALIDATION (MANDATORY)

You must confirm that:

* lead submitted from mobile → appears in admin on desktop
* change in admin → reflects on all devices
* notes → visible across devices
* offers / flights → synced everywhere

No local-only behavior allowed.

---

## 3. NOTIFICATION SYSTEM STILL BROKEN

Even after fixes:

* bell does NOT update
* no unread count
* no sound

This is because polling alone is NOT triggering notification events.

### Requirement:

You must:

* detect NEW leads (not just fetch list)
* compare previous vs new state
* trigger notification on difference

Polling is acceptable,
BUT notification must trigger when new data arrives.

---

## 4. PRODUCTION ≠ DEV

You must explicitly confirm:

* system works WITHOUT:

  * dev-api-server
  * vite proxy
  * .env.local

* all API routes work in Vercel serverless

---

## 5. FAILURE IS NOT ACCEPTABLE

The following must NEVER happen in production:

* leads only visible on one device
* admin changes not reflected on site
* notifications not triggering
* KV failing silently
* system relying on localStorage

---

## 6. REQUIRED CONFIRMATION

Before deployment, I need confirmation that:

1. KV is fully connected and required
2. cross-device sync works
3. notifications trigger on new leads
4. system behaves identically in production

---

This must be a real production system, not a dev simulation.

Everything must work reliably. No fallback illusions.

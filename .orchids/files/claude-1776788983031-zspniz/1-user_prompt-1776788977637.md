# JetX.vip — Admin Route Fixes (Next.js Migration Cleanup)

The public site design now looks correct, but the admin login route still has migration issues.

Please fix the admin-related issues below without changing the visual design.

## 1. Fix runtime error: `Navigate is not defined`

In `src/page-components/Login.jsx`, the login page is still using React Router style navigation:

```jsx
if (!loading && isAuthenticated) return <Navigate to={`/${ADMIN_PATH}/panel`} replace />;
```

This is invalid in Next.js.

### Required fix:

Replace the old React Router navigation with proper Next.js navigation.

Use one of these approaches safely:

* `useRouter()` from `next/navigation` and redirect inside `useEffect`
* or another correct Next.js-compatible redirect pattern

Important:

* do not break auth flow
* do not change the visual design
* do not refactor unrelated code

---

## 2. Fix Next.js 15 params usage warning

In `src/app/[adminSlug]/page.jsx`, the code is directly accessing:

```jsx
params.adminSlug
```

Next.js 15 warns that `params` must be unwrapped properly.

### Required fix:

Update the route implementation to the correct Next.js 15 pattern for dynamic route params.

Do this cleanly and safely.

---

## 3. Verify hydration warning source

There is also a hydration warning showing an extra attribute like:

```html
fdprocessedid="..."
```

This looks like a browser/dev-environment injected attribute rather than a real app bug.

### Required action:

Verify whether this warning reproduces:

* in production
* in incognito
* with browser extensions disabled

If it only appears in Orchids/dev or due to browser injection, do not over-engineer a fix.

Just report it clearly as environment-only.

---

## Deliverable

When done, send me:

1. exact files changed
2. exact code fix applied
3. confirmation that admin login works correctly
4. confirmation that public site design remains unchanged
5. whether the hydration warning is real or only dev/environment-related

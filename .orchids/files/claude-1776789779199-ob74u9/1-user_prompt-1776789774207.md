# JetX.vip — Admin Panel Runtime Fix (Blocking Issue)

The public site is fine, but the admin panel is still broken.

There are two remaining migration issues in the admin area, and one of them is currently blocking the panel from loading.

## 1) Blocking runtime error — `navigate is not defined`

In:

`src/page-components/Admin.jsx`

the code still references `navigate` even though this is now a Next.js app.

From the runtime error:

* `navigate is not defined`
* the failing area is around the `useEffect` dependency array and auth redirect logic

Example shown in the current code path:

```jsx
useEffect(() => {
  if (!isAuthenticated) router.push(`/${ADMIN_PATH}`, { replace: true });
}, [isAuthenticated, navigate]);
```

### Required fix:

* remove all remaining `navigate` references
* use only proper Next.js navigation
* if `router` is already used, the dependency array must reference `router`, not `navigate`
* audit `Admin.jsx` and `Login.jsx` for any remaining React Router navigation leftovers

Do NOT change design.

---

## 2) Next.js 15 params fix in admin panel route

In:

`src/app/[adminSlug]/panel/page.jsx`

the route still accesses:

```jsx
params.adminSlug
```

This is producing the Next.js 15 warning that `params` must be unwrapped properly.

### Required fix:

Update both admin route files to the correct Next.js 15 pattern:

* `src/app/[adminSlug]/page.jsx`
* `src/app/[adminSlug]/panel/page.jsx`

Do this safely and consistently.

---

## Deliverable

When done, send me:

1. exact files changed
2. exact code fix applied
3. confirmation that admin login works
4. confirmation that admin panel loads successfully
5. confirmation that the public site design remains unchanged

# Vercel Deployment Failure — Duplicate API Functions / EEXIST symlink

Deployment on Vercel failed.

The build compiles successfully and generates static pages, but deployment fails at the serverless functions step with:

`Error: EEXIST: file already exists, symlink 'auth.func'`

This looks like duplicate Vercel functions caused by having both:

* legacy root `/api` functions
* new Next.js App Router API routes under `src/app/api`

## Required fix

Remove or disable the legacy root `/api` folder if the Next.js `src/app/api/*/route.js` routes are now the source of truth.

Specifically check for duplicate function names such as:

* auth
* verify
* logout
* store
* health
* leads

## Requirements

1. Keep the Next.js App Router API routes.
2. Remove the old legacy `/api` functions from deployment.
3. Do not break admin login.
4. Do not break KV/store APIs.
5. Do not change visual design.
6. Ensure Vercel build/deploy succeeds cleanly.

## Also clean up if needed

* Make sure `.next/` is not committed.
* Make sure Vercel is not deploying duplicate generated function artifacts.
* Check `vercel.json` for old API/function routing rules that may conflict with Next.js.

## Deliverable

Send:

1. exact root cause
2. files/folders removed or changed
3. confirmation that only one API implementation remains
4. confirmation that admin/API still work
5. confirmation that Vercel deploy succeeds

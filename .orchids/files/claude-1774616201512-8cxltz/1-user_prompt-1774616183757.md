JetX.vp — Notification System Still Not Working (Must Be Properly Wired)

The notification system (bell + sound) is still NOT working, even after the previous fix.

Current behavior:

* New lead appears in the Leads list ✔️
* BUT:

  * Bell does NOT update ❌
  * No unread indicator ❌
  * No sound ❌

This confirms that the notification system is NOT connected to the actual lead creation flow.

---

## THIS IS THE ROOT PROBLEM

The system currently:

* renders leads correctly
* BUT does not emit any event or trigger when a new lead is created

As a result:

* notification system never updates

---

## REQUIRED IMPLEMENTATION (MANDATORY)

You must connect the notification system to the actual lead data flow.

---

## 1. Detect New Leads Properly

When the leads list updates (from KV/API/polling):

You must detect NEW leads by comparing:

* previous leads state
* new leads state

Example logic:

* if newLeads.length > previousLeads.length → new lead arrived
* OR detect new IDs not seen before

---

## 2. Trigger Notification Event

When a new lead is detected:

You MUST trigger:

* increment unread counter
* update bell UI
* play sound
* show toast (already working)

This must be done in the SAME place where leads are updated.

---

## 3. Bell State Must Be Connected to Leads State

The bell must NOT have its own disconnected state.

Instead:

* it must derive unread count from leads data
* or from a shared state updated when leads change

Right now, the bell is visually present but logically disconnected.

---

## 4. Sound Must Trigger on Real Event

Sound must be triggered ONLY when a NEW lead is detected.

Requirements:

* must work when admin is open
* must not require refresh
* must not depend on user interaction AFTER load

If browser requires user interaction for audio:

* unlock audio context on first click (standard Web Audio fix)

---

## 5. Must Work Without Refresh

Test scenario:

1. Open admin panel
2. Leave it open
3. Submit new lead from another device

Expected:

* lead appears in list ✔️
* toast appears ✔️
* bell updates ❗
* unread badge appears ❗
* sound plays ❗

ALL must happen immediately, without refresh.

---

## 6. Do NOT Fake This

This must NOT be:

* hardcoded
* manually triggered
* UI-only

It must be:

* data-driven
* based on real lead creation events

---

## 7. Debug Requirement

If needed, log clearly:

* when leads are fetched
* when a new lead is detected
* when notification is triggered

Right now, clearly the detection step is missing.

---

## EXPECTED RESULT

A new lead must trigger:

1. Leads list update ✔️
2. Toast ✔️
3. Bell update ❗
4. Unread badge ❗
5. Sound ❗

Currently steps 3–5 are NOT working and must be fixed properly.

This is a core admin feature and must behave reliably.

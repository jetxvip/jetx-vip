ORCHID UPDATE PROMPT – GLOBAL DATA SYNC + CONSISTENCY ACROSS DEVICES

Please perform a full system validation to ensure that all website data, admin-controlled content, and user-facing functionality are fully synchronized across all devices, browsers, and environments.

==================================================
GOAL
====

Ensure that any change made in the admin panel is immediately and consistently reflected across:

* all users
* all devices (mobile, desktop, tablet)
* all browsers
* incognito / private mode
* all geographic locations
* production environment

The website must behave identically and consistently for every user accessing it.

==================================================
CRITICAL REQUIREMENT
====================

No user should see outdated, cached, or inconsistent data.

All users must always see the latest version of the site content after admin updates.

==================================================
SYNC REQUIREMENTS (MUST VERIFY)
===============================

1. ADMIN → FRONTEND SYNC

Verify full data flow:

* Update content in admin
* Save successfully
* Data persists correctly in backend/database
* Frontend fetches updated data
* Live site reflects changes immediately or within acceptable short delay

This must work for:

* legal pages (Terms, Privacy, Accessibility)
* footer content
* phone numbers
* email addresses
* content sections
* language content
* cookie text/settings
* accessibility content
* any editable CMS fields

==================================================
2. CROSS-DEVICE CONSISTENCY

Verify that the same content appears identically across:

* mobile devices
* desktop devices
* tablets

No device-specific mismatches.
No missing content.
No layout-dependent data issues.

==================================================
3. CROSS-BROWSER CONSISTENCY

Verify behavior in:

* Chrome
* Safari
* Firefox
* Edge

Ensure:

* same content
* same language rendering
* same functionality

==================================================
4. INCOGNITO / PRIVATE MODE

Verify that:

* site loads correctly without prior cache
* all content appears correctly
* no dependency on previous session data
* no missing content
* no broken API calls

==================================================
5. CACHE HANDLING (CRITICAL)

Check if any caching layer exists (browser cache / CDN / server cache).

If caching is used:

* ensure cache invalidation works correctly after admin updates
* ensure users do NOT see outdated content
* ensure fresh data is fetched properly

If needed:

* implement cache-busting strategy
* use versioning or revalidation logic

==================================================
6. DATA SOURCE VALIDATION

Ensure that ALL dynamic content is loaded from the correct source:

* backend / CMS / API
* NOT hardcoded fallback values

There must be no case where:

* admin updates data
* but frontend still shows old hardcoded value

==================================================
7. MULTI-LANGUAGE SYNC

Verify that Hebrew and English versions are synchronized correctly:

* updating content in one language does NOT break the other
* correct language is always shown based on mode
* no fallback to wrong language
* no mixed-language UI

==================================================
8. DEPLOYMENT CONSISTENCY

Verify that:

* development behavior = production behavior
* no “works locally but not in production” issues
* environment variables are correct
* API endpoints are correct
* no broken production-only bugs

==================================================
9. REAL-TIME / NEAR REAL-TIME BEHAVIOR

Define expected update behavior:

* If real-time updates are not implemented, ensure updates appear after refresh
* If caching exists, ensure delay is minimal and controlled
* No long propagation delays

==================================================
10. FINAL VALIDATION TEST

Perform this exact test:

1. Open site on Device A
2. Open site on Device B (different network or incognito)
3. Change content in admin
4. Refresh both devices

EXPECTED RESULT:

* both devices show identical updated content
* no mismatch
* no delay issues

==================================================
FINAL REQUIREMENT
=================

Do not mark this task as complete until:

* all admin-controlled content is fully synced across devices
* no stale data appears anywhere
* no caching issues remain
* no environment inconsistencies exist
* production behaves exactly as expected

==================================================
EXPECTED FINAL OUTCOME
======================

* fully synchronized system
* consistent user experience across all devices
* reliable admin-to-frontend updates
* no data inconsistencies
* no cache-related bugs

END.

# Prompt 1 — Build the SEO Engine Only (No Content Bulk Yet)

We need to move faster and avoid long incomplete runs.

From now on, do NOT attempt the full 160-page rollout in one pass.

Your task in this prompt is ONLY to build the SEO page system/infrastructure.

## Scope of this prompt

Do ONLY the following:

1. Create the reusable multilingual SEO page architecture in Next.js
2. Create the routing structure for secondary SEO pages
3. Create the shared template/component system
4. Create the page data/config structure
5. Implement per-page metadata support
6. Implement canonical support
7. Implement hreflang support
8. Ensure sitemap can include these pages
9. Keep the current public design exactly unchanged

## Do NOT do in this prompt

* do NOT generate all 160 pages yet
* do NOT try to fill all 40 slugs yet
* do NOT do a giant content pass
* do NOT touch the main navigation
* do NOT redesign anything
* do NOT do unrelated refactors

## Required route structure

Use language-based routes for SEO pages, for example:

/en/private-jet-london
/he/private-jet-london
/ar/private-jet-london
/ru/private-jet-london

## Required output at the end

When finished, send me a short summary with:

1. which files were created
2. which files were modified
3. where the SEO content data lives
4. how new SEO pages are added
5. confirmation that no design changes were made

## Important execution rule

Complete this batch fully before stopping.
Do not pause midway.
If you must stop for any reason, send a checkpoint summary first:

* completed
* in progress
* pending

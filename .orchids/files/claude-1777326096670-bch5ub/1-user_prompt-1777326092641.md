The YouTube hero video works in Israel on desktop, iPhone, and Android, but fails in Cyprus on iPhone/iPad. It briefly shows the YouTube play icon/video background, then falls back to the image.

This means the code is mostly correct, but YouTube embed autoplay is not reliable enough globally for a hero background.

Please implement a production-safe video source strategy:

1. Add support for a direct hero video file URL:
   company.heroVideoMp4Url

2. If company.heroVideoMp4Url exists, use a native HTML5 <video> element instead of YouTube:
   - autoplay
   - muted
   - loop
   - playsInline
   - preload="auto"
   - poster={fallback image}
   - object-fit: cover
   - absolute full hero background
   - z-index: 1
   - fallback image behind it at z-index: 0

3. Keep YouTube only as secondary fallback:
   Priority should be:
   A. heroVideoMp4Url direct video
   B. YouTube heroVideoUrl
   C. fallback image

4. For the native video:
   - onCanPlay or onPlaying should mark video as working
   - onError should fall back to YouTube if available
   - if YouTube also fails, show fallback image

5. Do not rely on YouTube for the primary hero background anymore.
   YouTube can be region/device/autoplay restricted, especially on iOS/iPadOS and outside Israel.

6. Preserve the existing design, overlays, text, buttons, gradients and layout.

7. Add admin/company config field so I can paste a direct MP4 URL for the hero video.

Goal:
The hero video must work globally on iPhone, iPad, Android and desktop. Use native MP4 video as the primary source because YouTube embeds cannot be guaranteed worldwide.
# JetX.vip — Verify Real Production Navigation Speed

The site still feels very slow when navigating between pages in the browser opened from Orchids/dev environment.

Each route change takes around 4 seconds.

There is also a small “N” Next.js Route Info / Performance toolbar visible.

## First clarification required

Please confirm clearly:

1. Is the “N” toolbar only the Next.js development toolbar?
2. Will it be completely invisible in production?
3. Is the 4-second navigation delay dev-only, or does it reproduce in a production build?

---

## Required performance test

Do NOT judge performance only in dev mode.

Please run and test the real production build:

```bash
npm run build
npm run start
```

Then test navigation between:

* /
* /services
* /empty-legs
* /fleet
* /about
* /contact

Report actual navigation speed in production mode.

---

## If production is still slow

Audit and fix the cause.

Check specifically:

* remaining `force-dynamic` on public pages
* route-level dynamic rendering
* heavy client components
* unnecessary context re-renders
* intro wrapper rerendering on navigation
* language detection rerunning on navigation
* disabled Link prefetching
* large client bundles
* slow API calls blocking public page rendering
* layout/providers remounting between routes

---

## Important

Do NOT redesign anything.
Do NOT change visual styling.
Do NOT refactor unrelated code.

Only diagnose and fix real production navigation slowness.

---

## Deliverable

Send me:

1. whether the “N” toolbar is dev-only
2. production navigation test results
3. whether the slowness exists in production
4. root cause if production is slow
5. files changed, if any
6. confirmation that design remains unchanged
